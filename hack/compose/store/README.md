# Local development Store manifests for DP
# These stores map to local infrastructure managed by dp dev up.
#
# S3 stores     → LocalStack (localhost:4566)
# Kafka stores  → Redpanda   (localhost:19092)
# Postgres stores → PostgreSQL (localhost:5432)
