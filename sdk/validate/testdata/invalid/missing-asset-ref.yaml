apiVersion: data.infoblox.com/v1alpha1
kind: Model
metadata:
  name: model-missing-asset
  namespace: data-team
  version: 1.0.0
spec:
  description: A model referencing a missing asset
  owner: data-team
  runtime: generic-go
  image: myimage:v1
  mode: batch
  outputs:
    - name: customer-data
      type: s3-prefix
      binding: customer-data-bucket
      classification:
        sensitivity: public
        pii: false
