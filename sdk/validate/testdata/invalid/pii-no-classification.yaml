apiVersion: datakit.infoblox.dev/v1alpha1
kind: Transform
metadata:
  name: transform-missing-outputs
spec:
  runtime: generic-go
  image: myimage:v1
  mode: batch
  inputs:
    - dataset: source-data
  # outputs is missing - this should fail validation (E231)
