apiVersion: datakit.infoblox.dev/v1alpha1
kind: Transform
metadata:
  name: basic-pipeline
  namespace: data-team
  version: 1.0.0
spec:
  runtime: generic-go
  image: myimage:v1
  mode: batch
  inputs:
    - dataset: raw-data
  outputs:
    - dataset: customer-data
  schedule:
    cron: "0 * * * *"
