{{/* Templates directory for postgres wrapper chart. */}}
{{/* Custom deployment/service templates are delegated to the upstream bitnami/postgresql subchart. */}}
{{/* This file exists to ensure the templates directory is embedded. */}}
