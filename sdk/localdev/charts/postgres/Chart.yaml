apiVersion: v2
name: postgres
description: PostgreSQL for dp local development (upstream Bitnami subchart wrapper)
type: application
version: 0.2.0
appVersion: "17"

dependencies:
  - name: postgresql
    version: "18.3.0"
    repository: "oci://registry-1.docker.io/bitnamicharts"
