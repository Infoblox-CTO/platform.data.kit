{{/* Templates directory for redpanda wrapper chart. */}}
{{/* Custom deployment/service templates are delegated to the upstream subchart. */}}
{{/* This file exists to ensure the templates directory is embedded. */}}
