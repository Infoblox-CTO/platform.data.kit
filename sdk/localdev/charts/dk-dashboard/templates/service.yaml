apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: dk-dashboard
    app.kubernetes.io/name: dk-dashboard
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: dk-dashboard
    app.kubernetes.io/instance: {{ .Release.Name }}
