apiVersion: datakit.infoblox.dev/v1alpha1
kind: Transform
metadata:
  name: ingest
  version: 0.1.0
  labels:
    team: datakit
spec:
  runtime: cloudquery
  mode: batch
  inputs:
    - dataset: raw-events
  outputs:
    - dataset: raw-events-parquet
  trigger:
    policy: on-change
  timeout: 30m
