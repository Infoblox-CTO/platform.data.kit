apiVersion: data.infoblox.com/v1alpha1
kind: Bindings
metadata:
  name: test-pipeline-bindings
spec:
  bindings:
    - name: kafka-events
      s3Prefix:
        bucket: test-bucket
        prefix: data/
