"""
Example resource table and resolver for the {{.Name}} plugin.

Replace this with your actual data source table definitions.
"""
from typing import Generator

import pyarrow as pa
from cloudquery.sdk import schema
from cloudquery.sdk.scheduler import TableResolver


class ExampleResourceTable(schema.Table):
    """Example table — replace with your actual resource table."""

    def __init__(self) -> None:
        super().__init__(
            name="example_resource",
            title="Example Resource",
            description="An example resource table. Replace with your actual data source.",
            columns=[
                schema.Column(name="id", type=pa.string(), primary_key=True),
                schema.Column(name="name", type=pa.string()),
                schema.Column(name="value", type=pa.int64()),
                schema.Column(name="active", type=pa.bool_()),
            ],
        )


class ExampleResourceResolver(TableResolver):
    """Resolver that fetches example resources. Replace with your API client calls."""

    def resolve(self, client, parent_resource) -> Generator[dict, None, None]:
        """Yield rows as dictionaries matching the table columns."""
        # Replace this with actual API calls using the client
        yield {"id": "example-1", "name": "Example Item 1", "value": 100, "active": True}
        yield {"id": "example-2", "name": "Example Item 2", "value": 200, "active": False}
