"""
Client for the {{.Name}} plugin.

Holds connection state and configuration for API calls.
"""
from plugin.spec import Spec


class Client:
    """Client manages the connection/session for the {{.Name}} plugin."""

    def __init__(self, spec: Spec) -> None:
        self._spec = spec

    def id(self) -> str:
        """Return a unique identifier for this client instance."""
        return "{{.Name}}"

    def close(self) -> None:
        """Clean up any open connections."""
        pass
