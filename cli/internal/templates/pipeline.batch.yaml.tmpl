apiVersion: data.infoblox.com/v1alpha1
kind: Pipeline
spec:
  image: "${REGISTRY}/{{ .Name }}:${VERSION}"
  mode: batch
  
  env:
    - name: LOG_LEVEL
      value: info
      
  # Batch-specific settings
  timeout: 1h
  retries: 3
  backoffLimit: 6
