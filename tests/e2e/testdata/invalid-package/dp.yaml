# Invalid package - missing required fields
apiVersion: data.infoblox.com/v1alpha1
kind: DataPackage
metadata:
  name: ""  # Empty name is invalid
spec:
  type: pipeline
  # Missing: description, version, namespace
